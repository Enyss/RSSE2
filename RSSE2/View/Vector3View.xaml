<UserControl x:Class="RSSE2.Vector3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RSSE2"
             xmlns:wpfTool="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <DockPanel Height="30">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
            <Grid Margin="2" Width="95">
                <Label>x :</Label>
                <wpfTool:SingleUpDown FormatString="F3" Value="{Binding X}" Increment=".1" HorizontalAlignment="Right" Width="75"/>
            </Grid>
            <Grid Margin="2" Width="95">
                <Label>y :</Label>
                <wpfTool:SingleUpDown FormatString="F3" Value="{Binding Y}" Increment=".1" HorizontalAlignment="Right" Width="75"/>
            </Grid>
            <Grid Margin="2" Width="95">
                <Label>z :</Label>
                <wpfTool:SingleUpDown FormatString="F3" Value="{Binding Z}" Increment=".1" HorizontalAlignment="Right" Width="75"/>
            </Grid>
        </StackPanel>   
    </DockPanel>
</UserControl>
