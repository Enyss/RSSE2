<UserControl x:Class="RSSE2.ScrubberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RSSE2"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <CheckBox VerticalAlignment="Center" Margin="3" IsChecked="{Binding Is_SPARE}"/>
            <Label>Spare</Label>
        </StackPanel>
        <Label>Position</Label>
        <local:Vector3View DataContext="{Binding Position}"/>
        <Label>Rotation</Label>
        <local:Vector3View DataContext="{Binding Rotation}"/>
    </StackPanel>
</UserControl>
